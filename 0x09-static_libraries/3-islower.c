#include "main.h"

/**
 * _islower - lowercase checck up
 * @c: the character checked
 *
 * Return: 1 if the character is lowercase, 0 otherwise
 */
int _islower(int c)
{
    return (c >= 'a' && c <= 'z');
}
